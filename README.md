Для локального запуска:

1)npm i
загрузка зависимостей

2)Инициализировать таблицы с данными

3)npm run dev/npm run start
запуск проекта


1)
/notification-tasks/collect

 по установленному крону в 1 минуту будет ходить в таблицу schedules с запросом где мы будем искать занятые слоты в период с данной даты до суток более(текущая дата +24часа), если нашлись слоты, то проверяем есть ли в таблице tasks задача с данным schedule_id, так же можно связать данный schedule_id из таблицы foriegn_keys с tasks, чтобы при удалении слота у нас удалялась задача из таблицы, далее если не нашлось в таблице tasks текущего schedule_id c уже созданной задачей, то мы добавляем в таблицу tasks необходимые данные для звонка - добавляем в payload phone из Patients таблицы так же surname name patronymic и постим эту всю информацию в tasks

2)
/notification-tasks/result

по данному пути мы будем проверять так же по крону в 1 минуту, результат наших звонков, если обновление есть то будем обновлять таблицу Calls
